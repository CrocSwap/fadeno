---
description: Link the Fadeno CLI onto PATH for this user.
argument-hint: [--codex|--claude]
---

Use the Fadeno **setup** skill. Detect the current host and run its resolved
bundled `setup --codex` or `setup --claude` command. Report where the CLI was
linked, whether that directory is on PATH, and any permission it added.

Request: $ARGUMENTS
