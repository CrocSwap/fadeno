# Fadeno native roles (v1)

Use the pinned Fadeno `code-change-review` playbook with its native worker/reviewer/judge role subagents. Record the normal ledger and artifacts. If this host cannot reliably execute native roles, mark the unit `unsupported` in operator notes and do not replace it with degraded execution. Do not change Fadeno instructions during a batch.
