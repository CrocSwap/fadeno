{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://fadeno.dev/schemas/run.schema.json",
  "title": "Fadeno Run",
  "description": "One concrete execution of a playbook. Written to .fadeno/runs/<id>/run.yaml. This is the file-backed run ledger that makes a run inspectable and is the seam a future compiled runtime reads/writes.",
  "type": "object",
  "required": ["run_id", "schema_version", "playbook", "status", "task", "started_at", "host"],
  "additionalProperties": true,
  "properties": {
    "run_id": { "type": "string", "minLength": 1 },
    "schema_version": {
      "const": "0.3",
      "description": "Run-ledger format version. 0.2 and unversioned pre-0.3 ledgers are readable only in explicit compatibility mode (--legacy); writers accept only 0.3."
    },
    "playbook": { "type": "string", "minLength": 1 },
    "status": { "enum": ["running", "completed", "failed", "aborted"] },
    "task": { "type": "string" },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": "string", "format": "date-time" },
    "host": {
      "type": "string",
      "description": "Execution host, e.g. codex, claude, cli."
    },
    "artifacts_dir": { "type": "string" },
    "current_step": { "type": ["string", "null"] }
  }
}
