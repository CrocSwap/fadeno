---
description: Fadeno command-delivery lane for workspace-read-only archetypes
mode: primary
permission:
  "*": deny
  read: allow
  glob: allow
  grep: allow
  list: allow
  webfetch: allow
  websearch: allow
  lsp: allow
  edit: deny
  bash: deny
  task: deny
---

Inspect the workspace and produce the requested report without modifying files.
