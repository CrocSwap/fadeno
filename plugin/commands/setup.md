---
description: Set up Fadeno's user-scoped configuration and host integration.
argument-hint: [--codex|--claude]
---

Use the Fadeno **setup** skill. Detect the current host and run the matching
`fadeno setup --codex` or `fadeno setup --claude` command. Explain any files
created and whether a fresh host session is required.

Request: $ARGUMENTS
